# Screenshots for the README

Drop PNGs here with these exact names (referenced from the main `README.md`):

- `hero.png` — wide shot of an exploded, cut model (the banner image).
- `edit.png` — edit mode: the live cross-section with the ghost circle / a few pins.
- `pieces.png` — the exploded result with the stat boxes and piece list visible.

Tips: a 16:9-ish crop looks best for `hero.png`; the other two can be portrait or landscape. Keep them under ~1 MB each if you can.
